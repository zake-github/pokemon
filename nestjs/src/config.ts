export const version = '1';

export const apis = [`/v${version}/search`, `/v${version}/random`, `/v${version}/detail/\\d{4,6}`, `/v${version}/ability`, `/v${version}/type`, `/v${version}/max_id`, `/v${version}/region`,]