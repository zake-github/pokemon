export const version = '1';

export const apis = [`/v${version}/search`, `/v${version}/random`, `/v${version}/detail/\\d{4,4}(_[1-9])?`, `/v${version}/ability`, `/v${version}/type`, `/v${version}/max_id`, `/v${version}/region`, `/v${version}/basic_data`,]